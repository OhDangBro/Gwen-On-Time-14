<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{users.username}} 
    </div>
    <div class="text">
      {{comment_content}}
    </div>
  </section>
  {{/each}}
</div>